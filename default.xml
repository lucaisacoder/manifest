<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- remote: name 名称，fetch 远程地址。可以有多个remote，通过project.remote可指定不同的remote -->
  <remote name="origin"
          fetch="https://github.com/lucaisacoder/"
          review="https://github.com/lucaisacoder/" />

  <!-- default: revision 指定分支，remote 指定remote name，sync-j 同步线程数 -->
  <default revision="master"
           remote="origin"
           sync-j="4" />

  <!-- project：path 本地相对路径，name 远程相对路径，完整路径为 remote.fetch + project.name -->
  <project path="proj_repo/cmake" name="proj_repo-cmake" />
  <project path="proj_repo/config" name="proj_repo-config" />
  <project path="proj_repo/output" name="proj_repo-output" />
  <project path="proj_repo/public" name="proj_repo-public" />
  <project path="proj_repo/core" name="proj_repo-core" />
  <project path="proj_repo/osal" name="proj_repo-osal" />
  <project path="proj_repo/app" name="proj_repo-app" />
</manifest>
